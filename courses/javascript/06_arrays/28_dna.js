let dnaPieces = ["A", "C", "G", "T"];
let myDNA = [];
let dnaString = "";


for (x = 0; x < 3; x++) {
  randomIndex = Math.floor(Math.random() * 4);
  dnaString = dnaString + dnaPieces[randomIndex];
}

for (i = 0)

console.log(myDNA);
