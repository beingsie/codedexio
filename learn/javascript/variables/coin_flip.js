// Display a message indicating coin flipping
console.log("Flipping coin.. 🪙");

// Generate a random number between 0 and 1
let num = Math.random();

// Check if the random number is greater than 0.5 (a 50% chance)
if (num > 0.5) {
    // If the condition is met, it's Heads
    console.log("It's HEADS");
} else {
    // If not, it's Tails
    console.log("It's TAILS");
}